This route isn't used. The site runs in blog-only mode; the docs plugin is
kept registered only so the search plugin's version-aware search bar doesn't
crash — see the comment in `docusaurus.config.ts`.
